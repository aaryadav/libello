import { styled } from '../stitches.config';
import { Box } from './base/box';


export const Footer = () => {
    return (
        <Box css={{ position: '' }}>
            footer
        </Box>
    );
};